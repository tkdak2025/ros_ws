# 운영 코드 반영 검증 요약 v4

기준일: 2026-09-25. 검사 구조 정리와 U01/U02 수정 후 수행한 자동검증의 공유용 요약이다. 원본 측정 데이터·테스트 로그·생성 결과 폴더는 Git에 포함하지 않는다.

## 반영 내용

- 장비·Recipe·시퀀스·HMI 통신 책임 분리와 비동기 모니터링.
- START 전 Work Access 확보, 검사 완료 후 Access 대기.
- U01: 수동 HOME은 현재 Access에 도달했으면 중복 Open·후퇴 없이 Home 경로 실행.
- U02: Ready/Entry 접근 미도달은 후속 파지·접촉 이동 차단 및 Main 정지·오류 기록.
- 접촉 Entry/Pull의 거리 미도달 처리와 Hard 파지 완료 실패의 포인트 복구 유지.
- 워크스페이스의 이전 tools 제거. 별도 PRJT_prototype Y/Z 시험은 변경하지 않음.

## 검증 결과

| 구분 | 통과 | 실패 | 미실행/건너뜀 |
|---|---:|---:|---:|
| 자동시험 | 257 | 0 | 0 |
| 정량 비교 | 72 | 0 | 0 |

원본 실행 시각은 2026-09-25 21:03:32 +09:00이다. 기록된 소스 SHA256은 `1d1b2d0347749a5534d82eac84ce3e7c39dbbc41a291fddbee4a1615b84580f5`이다. 코드에 합성 피드백을 넣는 시험과 격리된 모의 ROS 통합시험을 포함한다. 실물 운전·접촉 간섭 검증은 하지 않았다.

원본 보고서는 로컬 `results/checklist/20260925_210332_101794/report.md`, 재현 입력·시험 로그는 같은 폴더에 있다. 이전 문제 관찰 결과는 로컬 `results/implementation_review/20260925_U01_U02/`에 보존했다. 수정 전 재현 코드는 현행 회귀시험으로 사용하지 않는다.

## 재실행

워크스페이스 루트에서 실행한다.

```bash
source /opt/ros/jazzy/setup.bash
source install/setup.bash
python3 src/cable_inspection/cable_inspection/diagnostics/check_inspection.py --with-ros
```

## 남은 확인

U03의 추가 기능 범위 판단과 R01~R05의 실제 Tool 방향·25 mm 해제·30 mm 후퇴/간섭·파지·판정 적합성은 남아 있다. 자동시험 통과는 실물 적합성이나 제품 합격률을 뜻하지 않는다.

## 에뮬레이터 virtual 전체 Job 확인

2026-09-25에는 진단 메뉴 2에 실제 가상 bringup 연결 검증을 추가했다. 기존 ROS 도메인을 유지하고 `LOCALHOST`에서 BMW 레시피 3개 포인트를 자동 START하여 검사 완료·Work Access 복귀·`job_summary.json`의 `INSPECTION_COMPLETE` 기록을 확인했다. 오프라인 자동시험 249건과 정량 비교 72건도 통과했다.

가상 케이블에는 저항이 없어 세 포인트 모두 Pull 최대거리 25 mm에 도달해 제품 판정은 FAIL이었다. 이것은 시퀀스 실행 검증의 실패가 아니다. 로컬 실행 보고서는 `results/checklist/20260925_220915_244698/report.md`, 가상 실행 상세는 같은 폴더의 `virtual_result.json`·`virtual_launch.log`·`virtual_run/`에 있다. 가상 bringup이 준비되지 않은 경우 메뉴 2는 모션을 시작하지 않고 실패 사유를 기록한다.
