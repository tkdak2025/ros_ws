project: CCCIS
scope:
  job_unit: one_recipe_execution
  scheduler: false
  external_triggers_via_hmi: true

states:
  system: [SYSTEM_READY, RUNNING, PAUSED, STOPPED, ERROR]
  judgment: [PENDING, COMPLETED, ERROR]
  inspection_result: [PASS, FAIL, MISSING]
  pull_termination: [FORCE_LIMIT, MAX_DISTANCE, TIMEOUT, MOTION_ERROR]

parameters:
  max_escape_distance_mm: 30.0
  adaptive_grip:
    max_entry_distance_mm: 25.0
    timeout_sec: 10.0
    entry_force_guard_n: 5.0
  pull:
    max_distance_mm: 25.0
    timeout_sec: 10.0
    speed_mm_s: TBD
    required_force_n:
      LAN: 20.0
      USB: 12.0
  judgment:
    displacement_limit_mm: 5.0
    slip_width_threshold_mm: TBD

hmi_triggers:
  - START
  - PAUSE
  - RESUME
  - STOP
  - HOME_RETURN

point_runtime_fields:
  - point_id
  - motion_status
  - grip_width_hard
  - peak_pull_force
  - pull_displacement
  - termination_reason
  - grip_width_change
  - judgment_status
  - result
  - reason
  - log_saved
